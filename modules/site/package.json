{
  "name": "@shadcn-scalajs/site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:basecoat-styles": "node scripts/build-basecoat-styles.mjs",
    "build:registry": "node scripts/build-registry.mjs",
    "predev": "npm run build:basecoat-styles && node scripts/build-shadcn-presets.mjs && npm run build:registry",
    "prebuild": "npm run build:basecoat-styles && node scripts/build-shadcn-presets.mjs && npm run build:registry",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@scala-js/vite-plugin-scalajs": "^1.0.0",
    "@tailwindcss/postcss": "^4.3.3",
    "playwright": "^1.62.1",
    "postcss": "^8.5.25",
    "tailwindcss": "^4.3.3",
    "tw-animate-css": "^1.4.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "puppeteer": "^25.5.0",
    "qrcode": "^1.5.4"
  }
}
