#!/usr/bin/env bash
# franky-adapter: static analysis.
set -euo pipefail
cd "$(dirname "$0")/../.."

export PATH="$PATH:/Users/locgorilla/Library/Application Support/Coursier/bin"

echo "lint: scalafmt check"
sbt scalafmtCheckAll

echo "lint: packages/cli typecheck"
(cd packages/cli && npx tsc --noEmit)
