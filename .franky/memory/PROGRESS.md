# Progress — source of truth for task state

## Done

- **v1** (sbt monorepo: `core`/`ui`/`webcomponents`/`site`/`packages/cli`): 5 components (Button, Badge, Dialog, Accordion, DropdownMenu), `ScElementBase` Shadow-DOM wrapper base, working CLI (`init`/`add`), franky bootstrapped with real build/lint/test/verify scripts. Bugs found live and fixed: dialog close-event double-fire, dropdown-menu click-outside eating selections in Shadow DOM (`composedPath()` fix).
- **Basecoat CSS → Tailwind v4 migration** (complete): dropped all vendored/patched basecoat CDN CSS; components now carry shadcn/ui's own Tailwind utility classes directly (`button.tsx`/`badge.tsx`/etc., copied class-for-class); `modules/site` runs a real Tailwind v4 + PostCSS pipeline (`globals.css` with `@theme inline` tokens); `modules/core/DataAttrs` deleted (data-variant/data-size no longer needed). Scala component API unchanged throughout. `vendor/basecoat-source/` + `vendor/shadcn-source/` added as reference snapshots consumed by generator scripts (see `vendor/NOTICE.md`).
- **Grew to 46 components** covering the full basecoat surface (alert, avatar, breadcrumb, card, checkbox, combobox, command, form primitives, popover, select, sidebar, table, tabs, toast, tooltip, etc.), each with a `.registry.json` sidecar and a `sc-*` Web Component export via `ScPrimitives`. Added a bento-grid landing page (`Main.scala`) with working style-pack switching (8 packs, scoped radius/shadow/border/height tokens) and a documentation-style `/components/:name` route (sidebar nav, live preview, install/usage code blocks, dark mode, table of contents).
- **Gap analysis against shadcn/ui's real 63-item catalog**: found `AGENTS.md` badly stale (still said "5 components" when 46 existed) and 15 components genuinely missing (excluding the newest AI-chat additions — Attachment/Bubble/Marker/Message/Message Scroller — and non-component pages — Direction/Typography).
- **Docs-site bug fixes**: deleted dead/unwired duplicate code (`HomePage.scala`/`DashboardHelpers.scala`/`SvgIcons.scala`); wired the previously-unreachable `componentsGalleryPage()` in as the `/components` index; added 5 components missing from the sidebar despite having a working preview (Collapsible, Form, Radio, Range, Scrollbar); replaced hard-coded prev/next footer links with ones computed from `componentNavList`'s real order; replaced the sparse 4-case Usage-code placeholder with a full case per component kept in 1:1 correspondence with `liveExample()`; wired `build-registry.mjs` into `predev`/`prebuild` so it can't silently go stale.
- **Ported the 14-item gap** (Data Table intentionally skipped — a Table+TanStack-Table recipe, not a distinct primitive, same call basecoat makes): AspectRatio, Toggle, ToggleGroup, HoverCard, NavigationMenu (CSS `group-hover` reveal, not a second hand-rolled JS timer state machine), Menubar (thin wrapper reusing `DropdownMenu` per menu — trigger keeps `DropdownMenu`'s own button look, a known simplification), Sheet (reuses `Dialog.element`, positioned to a side via Tailwind), Calendar (from-scratch month grid on `js.Date`, single-date only, no java.time dep), DatePicker (Popover+Calendar+Button composition), Carousel (native scroll-snap, no embla), ContextMenu (hand-rolled Var/EventBus like DropdownMenu, `contextmenu`-triggered), InputOTP (N inputs bound to one `Var[String]`, auto-advance), Resizable (drag-handle split via `documentEvents`). All 60 registry items now exist; all wired into the sidebar/live-preview/usage-code system.
- **Live-browser-verified**: Calendar grid+selection, DatePicker composition, Sheet slide-in/close, ToggleGroup toggling, InputOTP auto-advance, Carousel scroll — all confirmed working, zero console errors. `ContextMenu` could not be verified this way (claude-in-chrome's right-click hits Chrome's native menu, not our page's `contextmenu` listener) — reviewed against the proven DropdownMenu pattern instead.
- `AGENTS.md` and `vendor/NOTICE.md` rewritten to match current reality (both still described the deleted basecoat-CDN-CSS architecture in detail). New `vendor/NOTICE.md` flags a **latent** recurrence of the old Shadow-DOM `:root`-vs-`:host` bug in `globals.css:66` that will resurface once `sc-components.css` is wired up.

## Next

- 14 new components have no `Sc*`/`ScPrimitives` Web Component wrapper yet (only `modules/ui` + docs page exist so far).
- `sc-components.css` still isn't built/wired for `ScElementBase` — when it is, apply the `:root`→`:host` fix in `vendor/NOTICE.md` *before* shipping, not after rediscovering it's broken.
- `ContextMenu.scala`'s right-click trigger needs a manual (non-automated) browser check.
- CLI has no package-rewriting — "own every line" needs import-rewriting or a real `core` publish.
- `scripts/test` only checks CLI-written files exist, not that they compile (full check is manual, documented in `AGENTS.md`).
- Data Table and the newest AI-chat shadcn/ui additions remain a deliberate scope cut, not an oversight.

## Blockers

- (none)
