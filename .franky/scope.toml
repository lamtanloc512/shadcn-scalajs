# Default-deny permission scope — widen per session/task as needed.

env = "sandbox"
egress = []
secrets = []
actions = ["edit", "run_tests"]

[repos]
read = [
    "/Users/elam/Projects/ui"
]
write = []
