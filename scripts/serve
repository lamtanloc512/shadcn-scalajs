#!/usr/bin/env bash
# franky-adapter: start the dev server (modules/site's Vite server).
# Native Laminar demo: http://localhost:4300/
# Web Component demo:  http://localhost:4300/plain-html-demo.html
set -euo pipefail
cd "$(dirname "$0")/../modules/site"

export PATH="$PATH:/Users/locgorilla/Library/Application Support/Coursier/bin"
exec npm run dev
